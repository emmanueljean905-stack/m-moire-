# ============================================================
# Nixpacks Configuration - Beautiful Women (Railway)
# Indique à Railway comment builder et lancer le projet
# ============================================================

[phases.install]
cmds = ["npm install --prefix backend"]

[start]
cmd = "node backend/server.js"
